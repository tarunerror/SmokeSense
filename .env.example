# SmokeSense Environment Variables

# Optional: Configure cloud sync (leave empty for offline-only mode)
# SYNC_API_ENDPOINT=https://your-api.com
# SYNC_API_KEY=your-api-key

# Optional: Analytics (not implemented by default)
# ANALYTICS_ENABLED=false

# Development
# EXPO_PUBLIC_ENV=development
